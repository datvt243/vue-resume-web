---
node: issue-16-20-ci-cd
worker: implementer
date: 2026-08-20
---

## Task
Fix [issue #16](https://github.com/datvt243/vue-resume-web/issues/16)
(deploy script dùng force push, không có rollback) + [issue #20](https://github.com/datvt243/vue-resume-web/issues/20)
(không có CI/CD pipeline) trong 1 lượt — theo yêu cầu operator, và bản
thân issue #20 đã tự ghi "Implement cùng với issue #16".

## Branch
`feature/ci-cd-github-actions` (checkout từ `main` trước khi đổi file —
đúng `BranchBeforeCode`). Đặt tên `feature/*` vì đây là năng lực MỚI thêm
vào dự án (CI/CD pipeline), không phải bugfix — theo convention branch đã
thống nhất, `feature/*` sẽ được GIỮ LẠI sau khi merge, không bị `/ship` tự
xoá.

## Diff
| File | Why |
|---|---|
| `.github/workflows/ci.yml` (mới) | lint + build, chạy trên `push`/`pull_request` vào `main` — đúng đề xuất trong issue #20, KHÔNG thêm bước typecheck (xem "Noticed, not done") |
| `.github/workflows/deploy.yml` (mới) | build + lint + `peaceiris/actions-gh-pages@v4`, chạy trên `push` vào `main`, publish `dist/` lên `gh-pages`. Thêm `permissions: contents: write` (bắt buộc để action push được, không phụ thuộc setting mặc định của repo) — issue #16 chỉ đề xuất `v3`, dùng `v4` (bản mới nhất còn maintain) |
| `deploy.sh` (xoá) | Đúng đề xuất "Sau đó xoá deploy.sh" trong issue #16 — deploy giờ tự động, không cần script thủ công |
| `.claude/skills/deploy/SKILL.md` | Viết lại hoàn toàn — `deploy.sh` không còn tồn tại nên skill cũ (chạy `./deploy.sh`) sẽ báo lỗi ngay. Skill mới: check status qua `gh run list --workflow=deploy.yml`, không tự trigger gì (deploy giờ tự chạy khi `/ship` merge vào `main`, không cần lệnh riêng) |
| `agent-hub/doctrine/MEMORY.md` | Row "Deploy" cập nhật: không còn `./deploy.sh`, giờ tự động qua Actions. Thêm row "CI" mới, ghi rõ lý do không có typecheck |
| `agent-hub/haven/workers/implementer/MEMORY.md` | Thêm correction nối tiếp entry SSH-workaround cũ (không xoá — append theo đúng rule "never delete"), ghi rõ `deploy.sh` đã bị xoá hẳn nên workaround đó không còn áp dụng được |
| `.claude/CLAUDE.md` | Dòng "Branch: main → auto-deploys..." cập nhật, bỏ nhắc `deploy.sh` |

## Command
```
npm run build
npm run lint
ruby -ryaml -e "YAML.load_file('.github/workflows/ci.yml'); YAML.load_file('.github/workflows/deploy.yml')"
```

## Output (đọc lại nguyên văn)
```
> vue-resume-web@0.0.0 build
> vite build
...
✓ built in 4.65s
```
```
> vue-resume-web@0.0.0 lint
> eslint src --ext .js,.ts,.vue

(không có output — exit 0)
```
```
YAML OK
```

## Acceptance
| Criterion | Evidence |
|---|---|
| `ci.yml`/`deploy.yml` là YAML hợp lệ | `ruby -ryaml` parse thành công cả 2 file, không lỗi |
| `deploy.sh` đã xoá | `git status --short` → `D deploy.sh` |
| Build/lint local vẫn xanh sau khi xoá `deploy.sh` (không có gì trong app code phụ thuộc file này) | `✓ built in 4.65s`, lint exit 0 |
| `/deploy` skill không còn tham chiếu file đã xoá | Đọc lại `SKILL.md` mới — không còn `./deploy.sh` ở đâu |

## Noticed, not done — GIỚI HẠN QUAN TRỌNG, đọc trước khi SEAL
- **CHƯA thể verify workflow thật sự chạy đúng trên GitHub Actions runner**
  — sandbox này không có GitHub Actions runner để test local (không có
  `act` hay tương đương cài sẵn). Chỉ validate được: (1) YAML syntax hợp
  lệ, (2) `npm run lint`/`npm run build` — 2 lệnh mà cả `ci.yml` lẫn
  `deploy.yml` đều gọi — chạy xanh ở local với cùng Node version giả định
  (workflow pin `node-version: '20'`, chưa verify local Node version có
  khớp — xem thêm bên dưới). Việc workflow thật sự chạy pass/fail chỉ biết
  được SAU KHI merge vào `main` và quan sát qua `gh run list`/`gh run
  view`. Đây LÀ MỘT GIỚI HẠN THẬT, không phải checklist item có thể tự
  đánh dấu "done" — verifier/operator cần quyết định có chấp nhận SEAL với
  giới hạn này hay đợi merge xong rồi quan sát run thật trước khi SEAL.
- KHÔNG thêm bước typecheck vào `ci.yml` dù issue #20 có đề xuất — verified
  `npx vue-tsc --version` lỗi `ERR_PACKAGE_PATH_NOT_EXPORTED` (version
  TypeScript của dự án không tương thích `vue-tsc` mới nhất qua npx). Thêm
  bước này vào sẽ làm CI đỏ ngay từ lần chạy đầu tiên — ngoài scope để tự
  ý fix (cần điều tra/pin version riêng, có thể lộ ra nhiều lỗi type ẩn
  sau migration #13, xem "Noticed, not done" của evidence #13).
- `.eslintrc.cjs` KHÔNG bật ESLint rule cho `.github/workflows/*.yml`
  (không phải file JS/TS/Vue) — không nằm trong phạm vi `npm run lint`,
  chỉ validate được bằng YAML parser thuần.
- `.claude/CLAUDE.md` còn nhiều chỗ khác đã lỗi thời sau các fix hôm nay
  (vd "TypeScript (partial — see gotchas)" ở dòng Stack, cả bảng "Known
  Bugs" đều đã fix/closed) — KHÔNG sửa toàn bộ file trong task này, chỉ
  sửa đúng dòng liên quan tới deploy.sh. Dọn toàn bộ CLAUDE.md cần task
  riêng nếu operator muốn.

## Seal gate
Không chạm outward-facing THẬT trong bước implement này (không
commit/push/deploy/API thật) — chỉ sửa file local trên branch riêng. Việc
xoá `deploy.sh` là xoá file local trên branch, chưa merge/push nên chưa
phải hành động outward-facing thật. Seal gate: none ở bước này — merge về
`main` (kích hoạt CI + Deploy workflow thật lần đầu) cần approval riêng
qua `/ship`, và ĐÂY MỚI LÀ LÚC workflow thật sự được thử nghiệm.

## Trạng thái
sealed_pending_verifier — LƯU Ý: khác các node trước, node này có một phần
"chưa verify được" (workflow chạy thật) nằm ngoài khả năng của cả
implementer lẫn verifier trong sandbox này. Đề xuất: verifier SEAL dựa
trên local evidence (YAML hợp lệ + lint/build xanh + diff đúng phạm vi),
rồi SAU KHI ship, chạy `gh run list`/`gh run watch` để xác nhận thật —
ghi thành một ghi chú bổ sung (không phải node mới, vì không phải
regression) nếu phát hiện workflow lỗi thật.
