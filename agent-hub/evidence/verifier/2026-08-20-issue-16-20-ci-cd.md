---
node: issue-16-20-ci-cd
worker: verifier
date: 2026-08-20
verdict: SEAL
---

## Acceptance criteria checked
1. Trace về đúng 1 node (`issue-16-20-ci-cd`) — OK, node có mặt trên diagram
   với state `IN_PROGRESS` trước khi verify.
2. Branch check (`NoMainEdit`, step 7b) — note ghi rõ branch
   `feature/ci-cd-github-actions`. Tự chạy `git branch --show-current` độc
   lập → khớp chính xác. Không phải `main`. Pass.
3. `ci.yml` — đọc trực tiếp: `on: push/pull_request branches: [main]`,
   `actions/checkout@v4`, `actions/setup-node@v4` (`node-version: '20'`,
   `cache: 'npm'`), `npm ci`, `npm run lint`, `npm run build`. KHÔNG có bước
   typecheck/vue-tsc — khớp claim. Tự chạy độc lập `npx vue-tsc --version`
   → tái hiện đúng lỗi claim: `Error [ERR_PACKAGE_PATH_NOT_EXPORTED]:
   Package subpath './lib/tsc' is not defined by "exports" in
   .../node_modules/typescript/package.json`. Claim thật, không bịa.
4. `deploy.yml` — đọc trực tiếp: `on: push branches: [main]`,
   `permissions: contents: write` ở top-level, checkout/setup-node/npm
   ci/lint/build giống `ci.yml`, sau đó `peaceiris/actions-gh-pages@v4` với
   `github_token: ${{ secrets.GITHUB_TOKEN }}`, `publish_dir: ./dist`. Khớp
   claim.
5. YAML hợp lệ — tự parse độc lập bằng `ruby -ryaml` (không tin claim của
   note): `YAML.load_file` cho cả 2 file → không lỗi, in "YAML OK". Pass.
6. `deploy.sh` đã xoá — `ls deploy.sh` → "No such file or directory". `git
   status --short` → `D deploy.sh`. Pass.
7. `.claude/skills/deploy/SKILL.md` — đọc lại toàn bộ: không còn tham chiếu
   `./deploy.sh` ở đâu, mô tả đúng `gh run list --workflow=deploy.yml` +
   `gh run watch`/`gh run view --log-failed`, ghi rõ "nothing here needs
   Seal Gate approval since it triggers no new outward-facing action" và
   "Don't re-trigger a deploy manually" — không tự trigger deploy. Pass.
8. `doctrine/MEMORY.md` diff — tự `git diff main` độc lập: row "Deploy"
   cập nhật đúng (không còn `./deploy.sh`, mô tả Actions tự động), thêm row
   "CI" mới giải thích rõ lý do thiếu bước typecheck. Pass.
9. `agent-hub/haven/workers/implementer/MEMORY.md` — tự `git diff main` độc
   lập: entry correction SSH-workaround cũ KHÔNG bị xoá (append-only rule
   giữ nguyên), có thêm câu "CẬP NHẬT (issue #16, cùng ngày): `deploy.sh`
   đã bị XOÁ hẳn..." nối vào cuối entry cũ. Pass.
10. `.claude/CLAUDE.md` — tự `git diff main` độc lập: dòng Branch không còn
    "manual via `deploy.sh` for now", thay bằng mô tả CI + Deploy qua GitHub
    Actions. Pass.
11. `npm run build` — tự chạy độc lập từ repo root: `✓ built in 4.57s`, chỉ
    có warning chunk-size pre-existing (không liên quan diff này). Xanh.
12. `npm run lint` — tự chạy độc lập: exit 0, không output. Xanh, khớp
    claim.
13. `git diff main --stat` — tự chạy độc lập (dùng `git add -N` tạm để bao
    gồm cả file mới chưa track, sau đó `git reset` trả lại trạng thái
    untracked ban đầu, không commit gì): file list đúng phạm vi — 2 workflow
    mới (`ci.yml` +27, `deploy.yml` +34), `deploy.sh` xoá (-25),
    `.claude/CLAUDE.md`, `.claude/skills/deploy/SKILL.md`,
    `agent-hub/doctrine/MEMORY.md`, `agent-hub/haven/workers/implementer/MEMORY.md`
    sửa, cộng diagram + evidence implementer. KHÔNG có `yarn.lock` hay
    `package-lock.json` nào trong diff — không lặp lại trap lockfile drift
    của node trước. Pass.
14. Tỷ lệ diff (`SmallestDiff`) — đúng phạm vi 2 issue đã gộp theo yêu cầu
    operator, không đụng trap nào khác ngoài scope (note tự nêu rõ
    `.claude/CLAUDE.md` còn nhiều chỗ lỗi thời khác nhưng KHÔNG sửa toàn bộ
    — đúng kỷ luật).
15. Quét 6 forbidden states — không chạm cái nào: có node trên diagram
    (không `ADHOC_WORK`), có evidence note (không `NO_EVIDENCE`), mọi output
    đã tự chạy lại độc lập và đọc nguyên văn, không bị cắt (không
    `EDIT_UNVERIFIED`), không có code lẫn vào `haven/` (không
    `CODE_IN_HAVEN`), PM status cập nhật ngay sau verdict này (không
    `DIAGRAM_DRIFT`), diff nằm trên `feature/ci-cd-github-actions`, không
    phải `main` (không `MAIN_EDIT`).
16. Seal gate — diff không chạm outward-facing thật (không commit/push/merge
    → main) ở bước implementer này; note ghi "Seal gate: none ở bước này".
    Khớp — không cần approval ở đây, merge thật qua `/ship` sẽ là bước
    outward-facing riêng.

## Giới hạn đã ghi nhận (KHÔNG phải lý do REOPEN)
Sandbox này không có GitHub Actions runner (không có `act` hay tương
đương) — không thể tự chạy thật `ci.yml`/`deploy.yml` trên GitHub. Đây là
giới hạn cấu trúc của môi trường, không phải thiếu sót của implementer:
mọi thứ CÓ THỂ verify local (YAML syntax, trigger/step config, các lệnh
`npm run lint`/`npm run build` mà cả 2 workflow gọi, absence của
`deploy.sh`, nội dung skill/doctrine/CLAUDE.md đã sửa) đều đã được verify
ĐỘC LẬP ở trên và khớp claim. **Việc CI/Deploy workflow thật sự chạy đúng
trên GitHub Actions VẪN CHƯA ĐƯỢC XÁC NHẬN** — chỉ biết được sau khi merge
vào `main` và quan sát `gh run list`/`gh run view` thật. SEAL này KHÔNG có
nghĩa là "đã confirm chạy production" — đã ghi rõ caveat này vào PM status
trên diagram để phiên sau không hiểu nhầm.

## Verdict
SEAL.
